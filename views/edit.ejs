<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-4 font-semibold inline-block" href="/logout">logout</a>
        </div>
        <h3 class="text-3xl text-zinc-500 font-semibold mb-8 ml-2">Edit your Post:</h3> 
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="what's on your mind" class="block p-3 w-1/3 resize-none bg-transparent border-4 border-zinc-700 rounded-md" name="content" required><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 w-44 block rounded-md mt-4" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit Post</title>
</head>
<body class="bg-zinc-900 text-white">

    <!-- Main Content Wrapper -->
    <div class="w-full min-h-screen p-10 flex flex-col items-center">

        <!-- Logout Button -->
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-4 py-2 font-semibold hover:bg-red-600 transition-all" href="/logout">Logout</a>
        </div>

        <!-- Edit Post Title -->
        <h3 class="text-3xl text-zinc-500 font-semibold mb-8 ml-2">Edit your Post:</h3>

        <!-- Edit Post Form -->
        <form method="post" action="/update/<%= post._id %>" class="flex flex-col items-center space-y-6 w-full max-w-lg">
            
            <!-- Textarea for Post Content -->
            <textarea placeholder="What's on your mind?" class="w-full p-4 bg-transparent border-4 border-zinc-700 rounded-md text-zinc-200 resize-none focus:outline-none focus:border-blue-500" name="content" required><%= post.content %></textarea>

            <!-- Submit Button -->
            <input class="w-44 px-4 py-2 bg-yellow-500 text-white font-semibold rounded-md hover:bg-yellow-600 transition-all" type="submit" value="Update Post">
        </form>
    </div>

</body>
</html>







