<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl text-zinc-500 font-semibold mb-4 ml-2">Login</h3> 
        <form action="/login" method="POST" class="flex flex-col w-1/4">
            <input class="px-3 py-2 outline-none mb-2 rounded-md bg-transparent border-2 border-zinc-800" type="email" placeholder="email" name="email" required>
            <input class="px-3 py-2 outline-none mb-2 rounded-md bg-transparent border-2 border-zinc-800" type="password" placeholder="password" name="password" required>
            <input class="px-3 py-2 bg-blue-500 rounded-md" type="submit" value="Login Account">
         </form>    
    </div>
</body>
</html> -->





<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>

    <style>
        /* Bubble animation */
        @keyframes bubbleRise {
            0% {
                transform: translateY(0) scale(0.8);
                opacity: 0.8;
            }
            50% {
                transform: translateY(-200px) scale(1.2);
                opacity: 0.5;
            }
            100% {
                transform: translateY(-400px) scale(0.8);
                opacity: 0;
            }
        }

        .bubble {
            position: absolute;
            background-color: rgba(0, 204, 255, 0.6);
            border-radius: 50%;
            animation: bubbleRise 8s infinite ease-in-out;
            opacity: 0;
            pointer-events: none;
        }

        /* Bubble size variations */
        .bubble:nth-child(1) {
            width: 60px;
            height: 60px;
            animation-duration: 8s;
            animation-delay: 1s;
            left: 20%;
        }

        .bubble:nth-child(2) {
            width: 80px;
            height: 80px;
            animation-duration: 9s;
            animation-delay: 2s;
            left: 40%;
        }

        .bubble:nth-child(3) {
            width: 100px;
            height: 100px;
            animation-duration: 10s;
            animation-delay: 3s;
            left: 70%;
        }

        .bubble:nth-child(4) {
            width: 90px;
            height: 90px;
            animation-duration: 7s;
            animation-delay: 4s;
            left: 10%;
        }

        .bubble:nth-child(5) {
            width: 70px;
            height: 70px;
            animation-duration: 8s;
            animation-delay: 5s;
            left: 60%;
        }
    </style>
</head>
<body class="bg-white text-gray-900 overflow-hidden">

    <!-- Bubble Container -->
    <div class="absolute top-0 left-0 w-full h-full z-0">
        <div class="bubble" style="top: 90%;"></div>
        <div class="bubble" style="top: 95%;"></div>
        <div class="bubble" style="top: 92%;"></div>
        <div class="bubble" style="top: 98%;"></div>
        <div class="bubble" style="top: 96%;"></div>
    </div>

    <!-- Main Container to Center Content -->
    <div class="flex items-center justify-center min-h-screen relative z-10">

        <div class="absolute top-4 right-4">
            <a class="bg-blue-500 hover:bg-blue-600 text-white rounded-md px-6 py-3 font-semibold" href="/">Home</a>
        </div>

        <!-- Login Form Box -->
        <div class="bg-gray-100 p-8 rounded-lg shadow-lg w-full sm:w-96">

            <!-- Login Title -->
            <h3 class="text-3xl text-center text-gray-800 font-semibold mb-6">Login</h3> 

            <!-- Login Form -->
            <form action="/login" method="POST" class="space-y-4">

                <!-- Email Input -->
                <input class="w-full px-4 py-2 bg-transparent border-2 border-gray-300 rounded-md text-gray-800 focus:outline-none focus:border-blue-500" type="email" placeholder="Email" name="email" required>

                <!-- Password Input -->
                <input class="w-full px-4 py-2 bg-transparent border-2 border-gray-300 rounded-md text-gray-800 focus:outline-none focus:border-blue-500" type="password" placeholder="Password" name="password" required>

                <!-- Submit Button -->
                <input class="w-full px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-md cursor-pointer transition-all duration-300" type="submit" value="Login Account">

                <div class="flex items-center w-full">
                    <hr class="w-full border-t-2 border-gray-400">
                    <span class="text-gray-500 px-2">or</span>
                    <hr class="w-full border-t-2 border-gray-400">
                </div>

                <a href="/register" class="w-full block px-4 py-2 bg-transparent border-2 border-blue-500 hover:bg-white text-blue-500 font-semibold rounded-md text-center mt-4 transition-all duration-300">
                    Create Account
                </a>
            </form>    

        </div>
        
    </div>

</body>
</html>
